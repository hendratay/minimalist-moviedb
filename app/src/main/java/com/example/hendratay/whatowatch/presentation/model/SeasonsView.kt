package com.example.hendratay.whatowatch.presentation.model

data class SeasonsView(val airDate: String?,
                       val episodeCount: Int,
                       val id: Int,
                       val name: String,
                       val overview: String,
                       val posterPath: String?,
                       val seasonNumber: Int)